namespace notification_service.Model
{
    public enum NotificationType
    {
        RESERVATION_REQUEST_CREATED,
        RESERVATION_CANCELED,
        HOST_GRADED,
        ACCOMODATION_GRADED,
        SUPER_HOST,
        RESERVATION_REQUEST_REPLY
    }
}
